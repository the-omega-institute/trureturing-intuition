You are proposing one member of a bounded concept neighborhood in a verifier-grounded
structural-intuition pipeline. Return exactly one canonical JSON object with schema
`intuition-proposal.v1`. Bind the supplied candidate edit and copy its candidate ID, the frozen
neighborhood ID and target node ID, the target/related endpoint node IDs, and the conjectured bridge
into their typed proposal fields. Use only artifact refs present in the supplied frozen state and
candidate universe. List evidence actually read, state a falsifier, preserve the three independent
discovery ledgers, and never claim certification. The department will reject the batch unless there
is exactly one proposal for every one of the frozen neighborhood's 5-12 members. No markdown and no
chain-of-thought.
